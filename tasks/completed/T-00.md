---
id: T-00
title: Monorepo skeleton + `just` + CI
status: done
deps: []
mvp: true
review_stops: [D]
---
## Goal
Stand up the Turborepo + pnpm workspace with `apps/{api,web,mobile,docs}`, `packages/{ui,
shared-types,api-client,prompts,testing}`, a root `justfile` (`just format|lint|typecheck|
test|dev`), and a minimal `.github/workflows/ci.yml` running the standard pipeline from
`standards/AGENTS.md` §Gating.
Links: design §8 row T-00; `ai_native_monorepo_architecture_blueprint.md` §Phase 1.

## Contract
- pnpm workspaces + `turbo.json` config; each app/package exposes `format/lint/typecheck/test`.
- CI gates: format → lint → typecheck → test → build, plus I3/I5 placeholder hooks.
- Nothing here owns a contract yet — that's T-01.

## Acceptance criteria
- [ ] `pnpm install` is clean and `just` exists + `just typecheck` runs on an empty tree.
- [ ] CI runs the pipeline and is green on an empty tree.
- [ ] `just` interface matches `standards/AGENTS.md` exactly.

## Tests required
None (scaffolding). Verify by running the `just` commands locally; record that in the handoff.

## Verification
Full `just` pipeline + `just check-env` (I5) once `.env.example` exists — initially a no-op that
must still pass.

## Handoff (at completion)
_What was created (dirs, `just` recipes), commands run + results, TODO for T-01._

### Handoff — T-00 DONE (STOP-D signed off by human; committed)

**Status: landed.** Human approved the stack (STOP-D) and the `check-*` mapping.
Registry: T-00 = `done`; T-01 (dep now satisfied) remains `ready` (its own STOP-A awaits).

**Created (skeleton only, no lockfile, no install — per I6):**
- `pnpm-workspace.yaml`, `package.json` (root, turbo+prettier+typescript devDeps declared
   but NOT installed), `turbo.json`, `tsconfig.json`, `tsconfig.base.json`, `README.md`.
- `justfile` — exact AGENTS.md §Gating/§Verification interface: `gates`/`format`/`lint`/
   `typecheck`/`test`/`build`/`dev`/`install` + `check-env`/`check-contract`/
   `check-invariants`/`check-all`.
   NOTE (approved): `just` forbids `:` in recipe names, so the canonical check recipes are
   `just check-env` / `just check-contract` / `just check-invariants` / `just check-all`
   (NOT the colon notation). AGENTS.md §Verification, every task card, and both prompt
   files use the hyphen form so future tasks adhere to it.
- `apps/{api(FastAPI+uv pyproject/.python-version),web,mobile,docs}/`,
   `packages/{ui,shared-types,api-client,prompts,testing}/` (lean package.json stubs;
   real tooling lands in later tasks). `packages/shared-types/CONTEXT.md` +
   `src/index.ts` placeholder. `.pi/prompts/next-task.md` (the loop prompt).
- `.github/workflows/ci.yml` — pipeline `format→lint→typecheck→test→build` + the three
   check hooks, in a `pipeline` job; `api` job runs uv+pytest (SKIP until T-10).
- `.env.example` (secret-free, D8) + `scripts/check-env.sh`, `check-contract.sh`,
   `check-invariants.cjs`, `run-turbo.sh`.

**Verification run (macOS; node 22, pnpm 12.3.4, just 1.58.0, python 3.14):**
- `just gates` → all five turbo gates **SKIP cleanly** (turbo not installed pre STOP-D):
   exit 0. (Real run gated on sign-off + `just install`.)
- `just check-env` → **I5 PASS** (documented ⊇ used; used=none yet).
- `just check-contract` → **I3 SKIP-green placeholder** (no shared-types schema until T-01).
- `just check-invariants` → **I4 PASS** over `seed/**` (3 fixed-hiit templates = 5 phases in
   order; `stretch-reset-15` tag-exempt per I7). I1/I2 still TODO (need `buildTimeline`, T-20).
- All `package.json` files valid JSON; `.github/workflows/ci.yml` tab-free.
- Pipeline gates `format/lint/typecheck/test/build` are **not runnable** this round (turbo
   absent by design, pending STOP-D + `just install`); they SKIP rather than error, so the
   skeleton is verifiably on its feet.

**Tests:** none (pure scaffolding — T-00 card says so)._TODO for T-01:_ author the Zod
contract in `packages/shared-types` + its Pydantic mirror + wire `check-contract` drift.
