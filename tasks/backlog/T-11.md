---
id: T-11
title: Seed catalog + 3 HIIT + stretch + plan from `seed/**`; open Postgres driver STOP-D + PG18 CI
status: ready
owner: agent
deps: [T-10]
mvp: true
blockers: []
review_stops: [S]
priority: 60
size_estimate: M
est_days: 1.5
---

## Pre-steps (must land first — these carry the T-10 "F-2" findings + PG18 risk)
1. **Declar `psycopg[binary]` in `apps/api/pyproject.toml`** — a STOP-D sign-off. Only then do the
   live-migration guard un-skip. (The driver was installed into the venv *only* for T-10's local
   round-trip; it is deliberately NOT in `pyproject`/`uv.lock` yet — no dependency added on T-10's
   unapproved surface.)
2. **Wire a `postgres:18` service + `DATABASE_URL` in the CI `api` job** (`.github/workflows/ci.yml`,
   already annotated for this) so `test_migration_applies.py` actually runs the upgrade/downgrade in CI.
   Baseline = PG18 (design §4): `uuidv7()` is core there.
3. Decide the **controlled-enumeration columns** (design §4 addendum: FK fact tables A vs.
   CHECK-constrained slug arrays B) — a human A/B ratify — *before* seeding user-prefs data, so the
    seed shape and any new tables are settled first.
4. Resolve **T-01b** (Pydantic contract-mirror import fix) if T-11's seed loader/validator imports the
   mirror under real pydantic.

## Goal
Load `seed/catalog.json` (muscle_groups, equipment, exercises) + the HIIT/stretch/plan workout files
into a fresh DB idempotently (a T-10 `0001` migration + a seed script/fixture). The 13 muscle groups
and 4 equipment are the closed controlled enumerations; cross-check every referenced slug resolves
(the catalog cross-check invariant, design §4 notes).

## Acceptance
- `uv run` seed against a PG18 DB creates the catalog with no duplicate/conflicting slugs.
- CI `api` job: `postgres:18` service + `DATABASE_URL` → `test_migration_applies.py` green (no skip).
- `psycopg[binary]` present in `pyproject.toml` + `uv.lock`.
- Catalog cross-check passes for every seed file (F-02 exercise→muscle-group→equipment slugs resolve).
- Re-runnable (idempotent / `INSERT ... ON CONFLICT DO NOTHING`) — a STOP-S human review before commit.

## Files
`apps/api/app/db/seed.py`, `apps/api/app/db/fixtures/*`, `.github/workflows/ci.yml` (PG18 service),
`apps/api/pyproject.toml` (`psycopg[binary]` + `uv.lock` — STOP-D), `apps/api/tests/test_seed.py`.
Depends on T-10; carries the T-10 handoff notes F-1 (→ T-01b) and F-2 (psycopg/PG18) forward.


## Status
- [x] **DONE** (agent): `apps/api/app/seed.py` — `check_seed` (DB-free CI gate) + `load_seed`/`seed_all` (PG, skips when no PG). 7 tests + 3 PG-smoke skips. See HANDOFF.md.
