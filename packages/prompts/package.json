{
    "name": "@coach/prompts",
    "version": "0.0.0",
    "private": true,
    "description": "Orchestrating prompts / skills: run-next-task loop, evaluate/generate agent skills.",
    "scripts": {
      "format": "prettier --check '**/*.md' 2>/dev/null || true",
      "lint": "echo '(prompts: no lint yet; T-00 skeleton)'",
      "typecheck": "echo '(prompts: no TS yet; T-00 skeleton)'",
      "test": "echo '(prompts: no tests; T-00 skeleton)'",
      "build": "echo '(prompts: no build; T-00 skeleton)'"
    }
}
