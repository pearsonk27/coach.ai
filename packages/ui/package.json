{
   "name": "@coach/ui",
   "version": "0.1.0",
   "private": true,
   "description": "Shared clock + UI core (T-21/T-40). Pure logic typechecks without react; the React/Expo wrappers are deps-pending.",
   "main": "src/index.ts",
   "types": "src/index.ts",
   "scripts": {
        "format": "node -e 0",
        "lint": "node -e 0",
        "typecheck": "cd ../../ && npx tsc -p packages/ui/tsconfig.json --noEmit",
        "test": "node test/clock.test.cjs",
        "build": "cd ../../ && npx tsc -p packages/ui/tsconfig.json",
        "dev": "node -e 0"
      },
   "dependencies": {
        "@coach/shared-types": "workspace:*"
    }
}
